## Required Packages

The following Python packages are required to run this project:

-   `python3-matplotlib`
-   `python3-scipy`
-   `python3-pandas`
-   `python3-opencv`

You can install these packages using the following command:

```bash
sudo apt install python3-matplotlib python3-scipy python3-pandas python3-opencv -y